## 1. 工作空间与运行基础

- [x] 1.1 初始化 pnpm TypeScript 工作空间，创建 `apps/console`、`apps/api`、`apps/worker`，以及共享的领域、数据库、集成、界面和配置包
- [x] 1.2 添加严格的 TypeScript、Lint、格式化、单元测试、构建配置，以及可在全部包中统一运行的工作空间脚本
- [x] 1.3 添加经过校验的环境配置和密钥提供器接口，确保凭据不会进入浏览器构建产物
- [x] 1.4 接入本地 SQLite（单文件）、用户已部署的 Redis 与 S3 兼容对象存储（MinIO），配置 API、Worker 和 Publisher 辅助进程与健康检查；不使用 Docker 部署
- [x] 1.5 添加持续集成检查，覆盖依赖安装、数据库生成产物、Lint、类型检查、单元测试、集成测试和生产构建

## 2. 持久化与领域模型

- [x] 2.1 定义小红书单平台工作流的领域枚举、状态转换、错误分类、命令、事件和策略类型
- [x] 2.2 创建内容任务、运行任务、步骤尝试、工作流事件、发件箱记录和触发幂等记录的数据库迁移
- [x] 2.3 创建查询计划、来源文档、重复聚类、事实、候选方向、内容制品和事实来源关联的数据库迁移
- [x] 2.4 创建草稿修订、平台账号、平台策略、发布任务、发布回执、系统设置和审计事件的数据库迁移
- [x] 2.5 添加包含事务、乐观版本、唯一约束、咨询锁辅助方法和安全脱敏序列化的仓储方法
- [x] 2.6 仅初始化非敏感默认配置，包括质量门槛、强制人工审核发布、搜索预算和版本化小红书策略
- [x] 2.7 添加迁移和仓储测试，覆盖唯一性、状态转换约束、修订冲突和审计数据保留

## 3. 可恢复工作流与控制 API

- [x] 3.1 实现事务性发件箱到 BullMQ 的分发，以及幂等步骤处理器中间件
- [x] 3.2 实现持久化工作流状态机、检查点推进、终态规则和重启恢复扫描
- [x] 3.3 实现受认证保护的 `POST /api/v1/runs`，支持人工和调度器触发，并执行小红书单平台校验
- [x] 3.4 实现调度器 Bearer 认证、幂等重放、载荷冲突检测和触发审计记录
- [x] 3.5 实现运行任务列表/详情、方向选择、安全重试和取消接口，并添加授权和状态守卫
- [x] 3.6 持久化有序工作流事件，并实现 SSE 重放、`Last-Event-ID`、心跳和终态事件行为
- [x] 3.7 添加工作流集成测试，覆盖人工选向等待、自动选向门槛、恢复、重复投递、重试和取消

## 4. 研究与来源链路

- [x] 4.1 定义搜索网关、抓取器、正文提取器、向量服务和 LLM 网关接口，并为测试提供确定性模拟实现
- [x] 4.2 实现结构化查询规划，保存语言、意图、可配置查询数量、用量计量和部分失败记录
- [x] 4.3 实现 Brave Search 适配器，确保搜索摘要只作为召回元数据，抓取页面才作为证据来源
- [x] 4.4 实现安全页面抓取、正文提取、规范 URL 处理、对象存储持久化和提示词注入隔离
- [x] 4.5 实现 URL、文本指纹和语义重复聚类，同时保留全部来源记录
- [x] 4.6 实现可配置的权威性、相关性、原创性、时效性、交叉佐证和语言评分，并确保主要来源优先
- [x] 4.7 实现带来源关联、置信度、主要来源支持、使用位置和无来源事实门槛的事实抽取与核验
- [x] 4.8 实现结构化候选方向的生成、评分、排序、自动门槛选择和人工等待行为
- [x] 4.9 添加研究流程测试，覆盖搜索部分失败、权威非中文来源、转载聚类、无来源事实和未达到质量门槛

## 5. 内容生成与审核

- [x] 5.1 实现规范文章的版本化提示词/配置加载，并保存供应商、模型、提示词、Token 和来源元数据
- [x] 5.2 实现小红书标题、正文、标签、媒体引用的适配和事实继承，且不执行第二次研究
- [x] 5.3 实现来源覆盖、隐私、敏感内容、版权提示、AIGC 标识和账号策略审核门槛
- [x] 5.4 实现人工审核模式的草稿创建，以及默认需要人工批准的自动模式守卫
- [x] 5.5 实现草稿列表/详情，以及自动保存和手工保存共用的已清洗乐观锁 PATCH 接口
- [x] 5.6 实现实际生效内容预览，展示字符计数、媒体顺序、AIGC 状态、策略版本和可操作的校验结果
- [x] 5.7 实现事务性、幂等的草稿批准流程，重新校验最新修订版本并最多创建一个发布任务
- [x] 5.8 添加内容和草稿测试，覆盖衍生稿无来源事实、内容清洗、修订冲突、门槛失败和重复批准

## 6. 小红书发布集成

- [x] 6.1 实现使用密钥引用、脱敏 API 模型、授权健康状态和人工处理状态的平台账号服务
- [x] 6.2 定义包含授权检查、校验、预览、发布和状态查询的 Publisher Adapter 契约，并提供端到端测试用模拟适配器
- [ ] 6.3 封装选定的 `xiaohongshu-mcp` 辅助进程，记录已验证的镜像引用和适配器契约
- [x] 6.4 实现 TypeScript MCP 适配器，包括严格的请求/响应映射、超时、脱敏日志和错误分类
- [x] 6.5 实现版本化策略解析，并在批准和发布前校验标题、正文、标签、媒体、封面、AIGC 标识和审核模式
- [x] 6.6 实现账号级 BullMQ 路由、可配置并发、令牌桶限制、有界抖动和适配器熔断
- [x] 6.7 实现稳定的发布幂等键、数据库咨询锁、回执优先持久化和已有回执短路处理
- [x] 6.8 实现状态核验和未知结果处理，禁止在外部调用结果不明确时盲目重新发布
- [x] 6.9 实现有界瞬时错误重试，并将授权、验证、策略、内容、选择器和未知结果错误转入人工处理
- [x] 6.10 添加适配器契约和发布测试，覆盖辅助进程中断、策略漂移、重复投递、响应丢失、重试安全和回执核验

## 7. 管理后台框架与界面规则

- [x] 7.1 构建响应式中文应用框架、路由、侧栏、顶部栏、面包屑、模态框基础能力和与原型一致的设计变量
- [x] 7.2 仅以 CSS/Webfont 方式安装 Font Awesome Free，并创建不导入 SVG 渲染器的无障碍图标基础组件
- [x] 7.3 添加自动化源码检查，在界面代码出现 SVG 文件/组件或作为装饰使用的硬编码 Emoji 时失败
- [x] 7.4 实现浅色、深色和跟随系统主题，本地只保存非敏感的主题偏好
- [x] 7.5 添加键盘焦点、模态框焦点锁定和恢复、无障碍名称、非纯颜色状态、窄屏布局和无障碍测试

## 8. 管理后台产品视图

- [x] 8.1 实现概览页，展示服务端计算的运行任务、草稿、发布、搜索、Token 和最近活动数据
- [x] 8.2 实现工作流搜索/筛选/列表和新建任务模态框，并且只提供小红书账号和平台选项
- [x] 8.3 实现工作流详情，展示持久化步骤、尝试、SSE 事件、用量、候选方向、重试、取消和人工处理指引
- [x] 8.4 实现研究资料视图，展示评分因子、重复聚类、抓取状态和事实使用位置
- [x] 8.5 实现草稿箱、富文本编辑器、自动保存状态、修订冲突恢复、校验信息和实际生效的小红书预览
- [x] 8.6 实现发布统计和队列/历史视图，展示账号、内容制品、尝试、回执、核验、错误分类和可执行操作
- [x] 8.7 实现非敏感模型别名、搜索预算、质量门槛、策略引用、默认审核模式、连接状态和调度器接口示例设置
- [x] 8.8 添加组件和浏览器测试，覆盖主要原型流程、键盘操作、服务端持久化和不显示后续平台控件

## 9. 安全、审计与可观测性

- [x] 9.1 添加运营人员和调度器授权中间件、资源级守卫，以及审计事件中的明确操作主体
- [x] 9.2 应用请求校验、响应脱敏、内容安全策略、富文本安全渲染、服务端请求伪造防护和密钥/日志扫描
- [x] 9.3 为 API、工作流、研究、生成、草稿批准和发布添加关联结构化日志与 OpenTelemetry 链路
- [x] 9.4 导出队列延迟、步骤耗时/失败、搜索/Token 用量、人工等待、发布结果、熔断状态和账号健康指标
- [x] 9.5 添加不可变审计覆盖测试，包含配置变更、方向决策、编辑、批准、重试、外部调用和人工解决操作

## 10. 端到端校验与发布

- [x] 10.1 使用模拟集成添加端到端场景，覆盖调度触发、研究、人工选向、草稿编辑、批准、发布、回执和 SSE 重放
- [x] 10.2 添加自动模式端到端场景，证明合格内容可以推进，并且任一质量或安全门槛失败都会阻止自动发布
- [x] 10.3 添加重启和重复投递混沌测试，证明已完成步骤和发布副作用不会被主动重复执行
- [x] 10.4 对照 `docs/原型.html` 校验管理后台页面覆盖，同时确认小红书单平台范围和图标库限制
- [x] 10.5 编写本地设置、外部调度器 curl 用法、密钥、策略更新、账号恢复、未知发布结果和发布熔断恢复文档
- [ ] 10.6 使用测试账号先执行禁用发布的影子验证，再执行仅人工批准试点，并记录生产启停与回滚清单
